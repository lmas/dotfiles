#!/usr/bin/env python

import subprocess
import time

try:
        while 1:
                tmp = time.strftime('%a, %Y-%m-%d %H:%M:%S %Z')
                subprocess.call(['xsetroot', '-name', tmp])
                time.sleep(60)
except:
        pass
subprocess.call(['xsetroot', '-name', ''])

